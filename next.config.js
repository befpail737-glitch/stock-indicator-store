// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // 如果你的产品图片来自外部域名，需要在这里配置
  // images: {
  //   domains: ['example.com'],
  // },
}

module.exports = nextConfig